import { Link } from "react-router-dom";

const Profile = () => {
    return (
        <div>
            Profile Update
            <Link to="/registration">Registration</Link>
            Payment
        </div>
    );
};

export default Profile;